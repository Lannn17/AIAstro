# v0.7.0 测试清单

> 每完成一项在 `[ ]` 改为 `[x]`。
> 前置条件：后端跑在 8001，前端跑在 5173，已登录。

---

## 1. 合盘（Synastry）核心

### 1.1 双盘输入表单
- [ ] 登录 → 合盘 Tab → 出现「本命盘 A」和「本命盘 B」两组输入表单
- [ ] 两组表单均可独立填写，字段无互相干扰

### 1.2 从已保存星盘加载
- [ ] 盘 A 下拉选择已保存星盘 → 所有字段自动填入（含**时分**，不是 00:00）
- [ ] 盘 B 下拉选择已保存星盘 → 所有字段自动填入（含**时分**，不是 00:00）
- [ ] 盘 A / B 可以选不同的星盘

### 1.3 相位表
- [ ] 输入双盘数据 → 点击计算 → 出现行星相位表
- [ ] 表中有相位类型（合相/三分/刑/…）、容许度数值
- [ ] ASC / MC 接触点出现在表中
- [ ] 双重相位（double whammy）有标记
- [ ] 相位方向（A→B / B→A）正确显示

### 1.4 SVG 双盘图
- [ ] 计算完成后出现同心圆 SVG 图
- [ ] 内外盘可区分（颜色/标注不同）
- [ ] 图不空白、不报错

### 1.5 RAG AI 解读
- [ ] 计算完成 → 点击「AI 解读」→ 出现加载状态
- [ ] 返回中文解读文本（不是空/500）
- [ ] 解读内容引用了书籍来源

### 1.6 访客态合盘
- [ ] 未登录 → 合盘 Tab → 可手动填写计算（无报错）
- [ ] 未登录 → 下拉框不显示已保存星盘（或下拉为空）

---

## 2. 星盘编辑（PATCH）

### 2.1 进入编辑模式
- [ ] 侧边栏加载一张已保存星盘 → 出现编辑按钮
- [ ] 点击编辑 → 表单填入该星盘所有字段（含**时分**）

### 2.2 保存更新
- [ ] 修改某字段（如姓名）→ 点击「保存更新」→ 侧边栏标签同步更新
- [ ] 刷新页面后修改持久化
- [ ] 更新后侧边栏不新增额外条目

### 2.3 取消编辑
- [ ] 点击编辑 → 点击「取消」→ 表单恢复，不发 API 请求

---

## 3. 访客侧边栏 + 多图 Session

### 3.1 GuestSessionList 显示
- [ ] 未登录 → 计算第一张盘 → 侧边栏出现「当前会话」列表
- [ ] 再计算第二张盘 → 列表中出现两张（不覆盖）

### 3.2 切 Tab 后 Session 保留
- [ ] 访客计算完星盘 → 切到行运 Tab → 切回星盘 Tab → 结果和 SVG 仍在

### 3.3 行运 Tab 访客下拉
- [ ] 访客算了 2 张盘 → 行运 Tab 下拉 → 显示所有会话星盘（标注「未保存」）
- [ ] 切换选择后行运计算使用对应星盘数据

---

## 4. 校正系统 — 模糊日期输入

### 4.1 中文自然语言日期
- [ ] 校正 Tab → 人生事件日期 → 输入「去年夏天」→ 解析为合理日期范围，不报错
- [ ] 输入「2020年初」→ 正确解析
- [ ] 输入「三年前」→ 正确解析，显示解析结果

### 4.2 精确日期不受影响
- [ ] 输入「2023-06-15」→ 正常解析，流程不变

---

## 5. RAG / 占星对话

### 5.1 基本问答
- [ ] 星盘 Tab → 计算盘 → 占星对话输入问题 → 返回中文回答
- [ ] 回答中有书籍引用标注（不是裸 Gemini 输出）

### 5.2 书名引用可读
- [ ] 引用来源显示为可读书名（不是 camelCase 文件名）

### 5.3 Admin 记录
- [ ] 访问 `/admin` → 明细记录表中有上述对话
- [ ] `category` 字段非空、分类合理

---

## 6. 移动端响应式

### 6.1 星盘 Tab（375px）
- [ ] Chrome DevTools 375px → 星盘 Tab → 表单/SVG/行星表无水平溢出

### 6.2 合盘 Tab（375px）
- [ ] 375px → 合盘 Tab → 双输入表单堆叠显示，不重叠

### 6.3 导航栏（375px）
- [ ] 375px → 顶部 Tab 导航 → 所有 Tab 可点击，不被截断

---

## 7. 回归测试

### 7.1 登录/登出状态切换
- [ ] 登录 → 切换几个 Tab → 登出 → 重新登录 → 无残留数据、无崩溃

### 7.2 HF Spaces 直接 URL 访问
- [ ] 浏览器直接访问 `lannn17-astro.hf.space/admin` → 正常加载（不 404）
- [ ] 浏览器直接访问 `lannn17-astro.hf.space/` → 正常加载

### 7.3 访客提交 → 审核流程
- [ ] 未登录 → 计算星盘 → 确认提交 → 登录 → 侧边栏出现「待审核」
- [ ] 点击批准 → 星盘移入正式列表

### 7.4 本命盘核心功能
- [ ] 登录 → 填入出生数据 → 计算星盘 → SVG 正常渲染
- [ ] 行星表数据显示完整（无 undefined）
- [ ] 保存星盘 → 出现在侧边栏

---

## 进度统计

完成：0 / 38
